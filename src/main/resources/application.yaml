spring:
  application:
    name: stockflow-api

  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

server:
  servlet:
    context-path: /api/v1

springdoc:
  api-docs:
    path: /docs

  swagger-ui:
    path: /swagger

logging:
  level:
    root: INFO